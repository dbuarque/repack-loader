import { RepackLoader } from '../src/app/repack-loader';

const should = require('should');

describe('RepackLoader', function() {
    it('should be and object', function () {
        console.log(process.env.PWD)
    });

});
